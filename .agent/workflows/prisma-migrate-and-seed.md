---
description: # Prisma Migrate and Seed
---


**Description**: Chạy migrate và seed data test (users, sample posts/tags).

1. Chạy `npx prisma migrate deploy` (hoặc dev nếu local).
2. Nếu có seed script → chạy `npx prisma db seed`.
3. Kiểm tra DB connection và count rows sample.
4. Artifact: prisma studio screenshot hoặc query result summary.